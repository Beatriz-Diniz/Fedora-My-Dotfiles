$scrPath = $HOME/.local/share/bin # set scripts path

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=HDMI-A-1, preferred, auto, auto
#monitor=eDP-1, preferred, auto, auto

monitor = ,preferred,auto,auto
#monitor=HDMI-A-1,preferred,auto,auto

# Environment variables and D-Bus configuration
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1 # 1 no monitor
env = MOZ_ENABLE_WAYLAND,1
env = GDK_SCALE,1 # 1 no monitor
env = SDL_VIDEODRIVER,waylandss
env = _JAVA_AWT_WM_NONEREPARENTING,1
env = CLUTTER_BACKEND,wayland
env = GDK_BACKEND,wayland,x11
env = PATH,$PATH:$scrPath


# Import and update environment variables
exec-once = systemctl --user import-environment XDG_SESSION_TYPE XDG_CURRENT_DESKTOP
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = dbus-update-activation-environment --systemd --all # for XDPH
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # for XDPH
exec-once = dbus-update-activation-environment --all

# Update D-Bus and xdg-desktop-portal
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP && echo "DBUS environment updated" >> /tmp/hyprland.log
exec-once = dbus-update-activation-environment --systemd --all && echo "DBUS environment fully updated" >> /tmp/hyprland.log
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP && echo "Imported environment" >> /tmp/hyprland.log
exec-once = systemctl --user restart xdg-desktop-portal && echo "xdg-desktop-portal restarted" >> /tmp/hyprland.log
exec-once = sleep 2 && systemctl --user restart xdg-desktop-portal-gnome && echo "xdg-desktop-portal-gnome restarted" >> /tmp/hyprland.log

# System applications
exec-once = waybar # launch the system bar
exec-once = blueman-applet # systray app for Bluetooth
exec-once = udiskie --no-automount --smart-tray # front-end that allows to manage removable media
exec-once = nm-applet --indicator # systray app for Network/Wifi
exec-once = dunst # start notification demon
exec-once = wl-paste --type text --watch cliphist store # clipboard store text data
exec-once = wl-paste --type image --watch cliphist store # clipboard store image data
exec-once = $scrPath/swwwallpaper.sh # start wallpaper daemon
exec-once = $scrPath/batterynotify.sh # battery notification
exec-once = swww-daemon -f xrgb
exec-once = swww restore
exec-once = pulseaudio --start
#exec-once = ~/.config/hypr/toggle_monitors.sh


# See https://wiki.hyprland.org/Configuring/Environment-variables/
input {
    kb_layout = br
    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0
    force_no_accel = 1
}

# See https://wiki.hyprland.org/Configuring/Keywords/#executing
device {
    name = epic mouse V1
    sensitivity = -0.5
}

# See https://wiki.hyprland.org/Configuring/Variables/
gestures {
    workspace_swipe = true
    workspace_swipe_fingers = 3
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/
dwindle {
    pseudotile = yes
    preserve_split = yes
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/
master {
   # new_is_master = true
}

# See https://wiki.hyprland.org/Configuring/Variables/
misc {
    vrr = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
    force_default_wallpaper = 0
}

xwayland {
    force_zero_scaling = true
}

# Source additional configuration files
source = ~/.config/hypr/animations.conf
source = ~/.config/hypr/keybindings.conf
source = ~/.config/hypr/windowrules.conf
source = ~/.config/hypr/themes/common.conf # shared theme settings
source = ~/.config/hypr/themes/theme.conf # theme specific settings
source = ~/.config/hypr/themes/colors.conf # wallbash color override
source = ~/.config/hypr/monitors.conf # initially empty, to be configured by user and remains static
source = ~/.config/hypr/userprefs.conf # initially empty, to be configured by user and remains static

# Note: as userprefs.conf is sourced at the end, settings configured in this file will override the defaults
